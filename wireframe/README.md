# wireframe/

Internal React + Vite mockups (not deployed), mirroring carest's `wireframe/`
folder. Use this space for clickable UI prototypes of the admin and LIFF screens
before building them for real in `admin/` and `web/`.
